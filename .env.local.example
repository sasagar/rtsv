# Environment variables for the Real-time Survey Tool
# This file should be copied to .env.local and filled with your actual values.

# Supabase Configuration
# These are required for connecting to your Supabase project.
NEXT_PUBLIC_SUPABASE_URL="YOUR_SUPABASE_URL"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Supabase Service Role Key (for server-side operations with elevated privileges)
# This key should be kept secret and never exposed to the client-side.
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# Supabase JWT Secret (for custom JWT verification if used)
# This secret is used for signing and verifying JWTs.
SUPABASE_JWT_SECRET="YOUR_SUPABASE_JWT_SECRET"

# Optional: Socket.IO server URL if it's hosted separately.
# If your Socket.IO server runs on the same Next.js server, this might not be needed.
# NEXT_PUBLIC_SOCKET_URL="http://localhost:3001"

# Add any other environment variables here as your project grows.